# Deploy Pipeline MCP Server

Exposes the improved Deploy Pipeline as a discoverable MCP tool.

Agents can call it directly for atomic commits, Vercel deploys, verification, and media uploads with full Own-the-Outcome gates.